"""Past-paper analysis: extract questions, map them to the corpus, score topic importance.

`extract` turns a paper PDF into questions, `map` links each question to the sections that
teach it, `score` turns that into the topic-by-year matrix the heatmap renders.
"""
