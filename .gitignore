# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.py~

# Distribution / packaging
.Python
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
tags
MANIFEST

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Dependency directories
node_modules/
jspm_packages/
# `bench build` symlinks the app's node_modules into public/; git sees a symlink,
# not a directory, so the `node_modules/` rule above does not cover it.
wikify/public/node_modules

# Frontend build output (SPA). www/wikify.html is emitted by the vite plugin.
frontend/dist/
wikify/public/frontend/
wikify/www/wikify.html
# Generated by unplugin-auto-import / unplugin-vue-components at build time.
frontend/auto-imports.d.ts
frontend/components.d.ts

# IDEs and editors
.vscode/
.vs/
.idea/
.kdev4/
*.kdev4
*.DS_Store
*.swp
*.comp.js
.wnf-lang-status
*debug.log

# Helix Editor
.helix/

# Aider AI Chat
.aider*

# POC playground — keep the app code, drop the heavy/sensitive bits
scratch/pdf_lab/.venv/
scratch/pdf_lab/storage/
pocs/gpt_parse_load/runs/
# Source PDFs (sensitive manuals) — never commit
files/
# Generated wiki pages contain verbatim source-manual content — keep out of public repo
reports/wiki/
!specs/gpt-poc/scratch/
!specs/gpt-poc/scratch/*.md

# agent verification screenshots (spec-loop)
scratch/shots/
/*.png
